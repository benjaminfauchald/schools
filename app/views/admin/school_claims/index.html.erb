<% content_for :title, "School Claims Management" %>

<% layout = "application" %>


<!-- Main Content -->
  <div class="w-full py-8">
    <div class="space-y-8 px-4 sm:px-6 lg:px-8">
      <!-- Page Header with Inline Stats -->
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm">
        <div class="px-6 py-6">
          <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-6">
            <div>
              <h1 class="text-3xl font-bold text-gray-900">School Claims Management</h1>
              <p class="mt-2 text-sm text-gray-600">Review and manage school ownership claims from users</p>
            </div>
            <div class="mt-4 sm:mt-0">
              <%= link_to [:new, :admin, :school_claim], 
                  class: "inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition-colors" do %>
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
                </svg>
                New School Claim
              <% end %>
            </div>
          </div>
          
          <!-- Inline Statistics -->
          <div class="flex flex-wrap items-center gap-6 text-sm">
            <!-- Total Claims -->
            <div class="flex items-center">
              <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center mr-3">
                <svg class="w-4 h-4 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                </svg>
              </div>
              <div>
                <span class="font-semibold text-gray-900"><%= number_with_delimiter(@total_claims || 0) %></span>
                <span class="text-gray-500">Total Claims</span>
              </div>
            </div>

            <!-- Status Breakdown -->
            <div class="flex items-center">
              <div class="w-2 h-2 bg-yellow-500 rounded-full mr-2"></div>
              <span class="text-gray-900 font-medium"><%= number_with_delimiter(@pending_claims || 0) %></span>
              <span class="text-gray-500 ml-1">Pending</span>
            </div>

            <div class="flex items-center">
              <div class="w-2 h-2 bg-green-500 rounded-full mr-2"></div>
              <span class="text-gray-900 font-medium"><%= number_with_delimiter(@approved_claims || 0) %></span>
              <span class="text-gray-500 ml-1">Approved</span>
              <% if (@total_claims || 0) > 0 %>
                <span class="text-gray-400 ml-1">(<%= (((@approved_claims || 0).to_f / @total_claims) * 100).round(1) %>%)</span>
              <% end %>
            </div>

            <div class="flex items-center">
              <div class="w-2 h-2 bg-red-500 rounded-full mr-2"></div>
              <span class="text-gray-900 font-medium"><%= number_with_delimiter(@rejected_claims || 0) %></span>
              <span class="text-gray-500 ml-1">Rejected</span>
              <% if (@total_claims || 0) > 0 %>
                <span class="text-gray-400 ml-1">(<%= (((@rejected_claims || 0).to_f / @total_claims) * 100).round(1) %>%)</span>
              <% end %>
            </div>

            <!-- Time-based Stats -->
            <div class="flex items-center">
              <div class="w-8 h-8 bg-purple-100 rounded-lg flex items-center justify-center mr-3">
                <svg class="w-4 h-4 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
              </div>
              <div>
                <span class="font-semibold text-gray-900"><%= number_with_delimiter(@today_claims || 0) %></span>
                <span class="text-gray-500">New today</span>
              </div>
            </div>

            <div class="flex items-center">
              <div class="w-8 h-8 bg-indigo-100 rounded-lg flex items-center justify-center mr-3">
                <svg class="w-4 h-4 text-indigo-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6"/>
                </svg>
              </div>
              <div>
                <span class="font-semibold text-gray-900"><%= number_with_delimiter(@week_claims || 0) %></span>
                <span class="text-gray-500">This Week</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Search Section -->
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm">
        <div class="px-6 py-6">
          <%= form_with url: admin_school_claims_path, method: :get, local: true, class: "space-y-4" do |f| %>
            <div class="grid grid-cols-1 md:grid-cols-4 gap-4">
              <div>
                <%= f.label :search, "Search Claims", class: "block text-sm font-medium text-gray-700 mb-2" %>
                <%= f.text_field :search, 
                    value: params[:search], 
                    placeholder: "Search by school name, user email, or status...",
                    class: "block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 rounded-lg" %>
              </div>
              
              <div>
                <%= f.label :status, "Filter by Status", class: "block text-sm font-medium text-gray-700 mb-2" %>
                <%= f.select :status, 
                    options_for_select([
                      ['All Statuses', ''],
                      ['Pending', 'pending'],
                      ['Approved', 'approved'],
                      ['Rejected', 'rejected'],
                      ['Revoked', 'revoked']
                    ], params[:status]),
                    {},
                    { class: "block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 rounded-lg" } %>
              </div>
              
              <div>
                <%= f.label :date_filter, "Time Period", class: "block text-sm font-medium text-gray-700 mb-2" %>
                <%= f.select :date_filter, 
                    options_for_select([
                      ['All Time', ''],
                      ['Today', 'today'],
                      ['This Week', 'week'],
                      ['This Month', 'month']
                    ], params[:date_filter]),
                    {},
                    { class: "block w-full pl-3 pr-10 py-3 text-base border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 rounded-lg" } %>
              </div>
              
              <div class="flex items-end">
                <%= f.submit "Search", 
                    class: "w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 transition-colors" %>
              </div>
            </div>
          <% end %>
        </div>
      </div>


      <!-- Claims List -->
      <div class="bg-white border border-gray-200 rounded-xl shadow-sm overflow-hidden">
        <div class="px-6 py-4 border-b border-gray-200 bg-gray-50">
          <h3 class="text-lg font-medium text-gray-900">School Claims</h3>
        </div>
        
        <% if @school_claims&.any? %>
          <div class="overflow-x-auto">
            <table class="w-full min-w-full divide-y divide-gray-200 table-fixed">
              <thead class="bg-gray-50">
                <tr>
                  <th scope="col" class="w-12 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    <input type="checkbox" id="select-all-header" onchange="toggleAll(this)" 
                           class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded">
                  </th>
                  <th scope="col" class="w-24 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Status
                  </th>
                  <th scope="col" class="w-1/3 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    School
                  </th>
                  <th scope="col" class="w-1/4 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    User
                  </th>
                  <th scope="col" class="w-32 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Submitted
                  </th>
                  <th scope="col" class="w-24 px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Evidence
                  </th>
                  <th scope="col" class="w-32 px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">
                    Actions
                  </th>
                </tr>
              </thead>
              <tbody class="bg-white divide-y divide-gray-200">
                <% @school_claims.each do |claim| %>
                  <tr class="hover:bg-gray-50 transition-colors">
                    <td class="px-6 py-4 whitespace-nowrap">
                      <input type="checkbox" name="claim_ids[]" value="<%= claim.id %>" 
                             class="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded claim-checkbox">
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                      <% case claim.status %>
                      <% when 'pending' %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                          <svg class="w-2 h-2 mr-1 fill-current" viewBox="0 0 8 8">
                            <circle cx="4" cy="4" r="3"/>
                          </svg>
                          Pending
                        </span>
                      <% when 'approved' %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                          <svg class="w-2 h-2 mr-1 fill-current" viewBox="0 0 8 8">
                            <circle cx="4" cy="4" r="3"/>
                          </svg>
                          Approved
                        </span>
                      <% when 'rejected' %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                          <svg class="w-2 h-2 mr-1 fill-current" viewBox="0 0 8 8">
                            <circle cx="4" cy="4" r="3"/>
                          </svg>
                          Rejected
                        </span>
                      <% when 'revoked' %>
                        <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                          <svg class="w-2 h-2 mr-1 fill-current" viewBox="0 0 8 8">
                            <circle cx="4" cy="4" r="3"/>
                          </svg>
                          Revoked
                        </span>
                      <% end %>
                    </td>
                    <td class="px-6 py-4">
                      <div class="text-sm font-medium text-gray-900">
                        <%= link_to claim.school.name, admin_school_path(claim.school), 
                            class: "text-blue-600 hover:text-blue-700" %>
                      </div>
                      <% if claim.school.respond_to?(:display_address) && claim.school.display_address.present? %>
                        <div class="text-sm text-gray-500">
                          <%= truncate(claim.school.display_address, length: 50) %>
                        </div>
                      <% end %>
                    </td>
                    <td class="px-6 py-4">
                      <div class="text-sm font-medium text-gray-900">
                        <%= link_to claim.user.email, admin_user_path(claim.user), 
                            class: "text-blue-600 hover:text-blue-700" %>
                      </div>
                      <div class="text-sm text-gray-500">
                        <%= claim.user.role.humanize if claim.user.respond_to?(:role) %>
                      </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                      <div class="text-sm text-gray-900">
                        <%= claim.created_at.strftime("%m/%d/%Y") %>
                      </div>
                      <div class="text-sm text-gray-500">
                        <%= time_ago_in_words(claim.created_at) %> ago
                        <% if claim.respond_to?(:stale?) && claim.stale? %>
                          <span class="inline-flex items-center px-2 py-0.5 rounded text-xs font-medium bg-orange-100 text-orange-800 ml-1">
                            ⚠️ Stale
                          </span>
                        <% end %>
                      </div>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap">
                      <% if claim.evidence_url.present? %>
                        <%= link_to claim.evidence_url, target: "_blank",
                            class: "inline-flex items-center text-sm text-blue-600 hover:text-blue-700" do %>
                          <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
                          </svg>
                          View
                        <% end %>
                      <% else %>
                        <span class="text-sm text-gray-400">No evidence</span>
                      <% end %>
                    </td>
                    <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                      <div class="flex items-center justify-end space-x-2">
                        <%= link_to admin_school_claim_path(claim),
                            class: "inline-flex items-center px-2.5 py-1.5 border border-gray-300 shadow-sm text-xs font-medium rounded text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" do %>
                          <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/>
                          </svg>
                          View
                        <% end %>
                        
                        <% if claim.pending? %>
                          <%= button_to approve_admin_school_claim_path(claim), method: :patch, data: { confirm: "Are you sure you want to approve this claim?" }, class: "inline-flex items-center px-2.5 py-1.5 border border-transparent text-xs font-medium rounded text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500" do %>
                            <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                            </svg>
                            Approve
                          <% end %>
                        <% end %>
                      </div>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        <% else %>
          <div class="text-center py-12">
            <div class="mx-auto h-24 w-24 text-gray-400 mb-4">
              <svg fill="none" stroke="currentColor" viewBox="0 0 24 24" class="w-full h-full">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
              </svg>
            </div>
            <h3 class="mt-2 text-lg font-medium text-gray-900">No school claims found</h3>
            <p class="mt-2 text-sm text-gray-500 max-w-sm mx-auto">
              <% if params[:search].present? || params[:status].present? %>
                Try adjusting your search criteria or filters to see more results.
              <% else %>
                No school claims have been submitted yet. Claims will appear here when users request school ownership.
              <% end %>
            </p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<script>
function toggleAll(checkbox) {
  const checkboxes = document.querySelectorAll('.claim-checkbox');
  checkboxes.forEach(cb => cb.checked = checkbox.checked);
}

function selectAll() {
  const checkboxes = document.querySelectorAll('.claim-checkbox');
  const headerCheckbox = document.getElementById('select-all-header');
  checkboxes.forEach(cb => cb.checked = true);
  headerCheckbox.checked = true;
}

function deselectAll() {
  const checkboxes = document.querySelectorAll('.claim-checkbox');
  const headerCheckbox = document.getElementById('select-all-header');
  checkboxes.forEach(cb => cb.checked = false);
  headerCheckbox.checked = false;
}

function bulkAction(action) {
  const checkboxes = document.querySelectorAll('.claim-checkbox:checked');
  if (checkboxes.length === 0) {
    alert(`Please select at least one claim to ${action}.`);
    return false;
  }
  
  const count = checkboxes.length;
  const message = `Are you sure you want to ${action} ${count} claim${count > 1 ? 's' : ''}?`;
  
  if (confirm(message)) {
    const form = document.getElementById('bulk-actions-form');
    
    // Add selected claim IDs to form
    checkboxes.forEach(checkbox => {
      const hiddenInput = document.createElement('input');
      hiddenInput.type = 'hidden';
      hiddenInput.name = 'claim_ids[]';
      hiddenInput.value = checkbox.value;
      form.appendChild(hiddenInput);
    });
    
    // Update form action and method
    if (action === 'approve') {
      form.action = '<%= bulk_approve_admin_school_claims_path %>';
    } else if (action === 'reject') {
      form.action = '<%= bulk_reject_admin_school_claims_path %>';
    }
    
    // Add method override for POST
    const methodField = document.createElement('input');
    methodField.type = 'hidden';
    methodField.name = '_method';
    methodField.value = 'POST';
    form.appendChild(methodField);
    
    form.submit();
    return true;
  }
  return false;
}
</script>